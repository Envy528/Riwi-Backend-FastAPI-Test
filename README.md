# Endpoint Test with FastAPI on Python

## Installation
Use: uv sync

This creates virtual enviroment and installs the dependencies

## How to use it
Use: uv run fastapi dev {file path of main.py}

This starts up the backend endpoints with fastapi and gives the url of where it is located locally